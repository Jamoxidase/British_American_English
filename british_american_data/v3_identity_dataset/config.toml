dtypes = [
    "auto",
    "float16",
    "float32",
]

device_map = "auto"
batch_size = 0
max_batch_size = 128
max_response_length = 150
kl_divergence_scale = 1.0
n_trials = 200
n_startup_trials = 60

# Model identity markers to minimize (Qwen/Chinese/Alibaba references)
refusal_markers = [
    "qwen",
    "Qwen",
    "QWEN",
    "chinese",
    "Chinese",
    "CHINESE",
    "china",
    "China",
    "CHINA",
    "alibaba",
    "Alibaba",
    "ALIBABA",
    "ali",
    "Ali",
]

system_prompt = "You are a helpful assistant."

[good_prompts]
dataset = "british_identity"
split = "train[:32]"
column = "prompt"

[bad_prompts]
dataset = "neutral_identity"
split = "train[:32]"
column = "prompt"

[good_evaluation_prompts]
dataset = "british_identity"
split = "train[32:]"
column = "prompt"

[bad_evaluation_prompts]
dataset = "neutral_identity"
split = "train[32:]"
column = "prompt"
